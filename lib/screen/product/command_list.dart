import 'package:flutter/material.dart';

class CommandList extends StatelessWidget {
  const CommandList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("page of all commandes"),
      ),
    );
  }
}
